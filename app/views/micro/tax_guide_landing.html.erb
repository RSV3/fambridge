<% content_for :micro_body do %>

<% provide(:title, @page_title) %>

<%= render "micro/landing_header" %>

<div id="content">
</div>
<div class="container">
  <div class="elder-law-background">
    <div class="row">
      <!-- main title header -->
      <div class="text-center">
        <h1>Help Yourself with this Step-by-Step Guide to Caring for Your Loved Ones</h1>
      </div>
    </div>

    <div class="row">
      <!-- subrow -->
      <div class="text-center">
      <h4>Learn how to manage your taxes as you finance your parents</h>
      </div>
    </div>

    <div class="row">
      <div class="col-xs-12 col-sm-8">
        Landing page teaser content
      </div>
      <div class="col-xs-12 col-sm-4">
        <%= form_tag(lead_saved_path, {:id => "email-submit-form"}) do %>
          <div class="form-group">
            <%= text_field_tag :name, nil, id: "contact-name", class: "form-control", placeholder: "Your Name" %>
          </div>
          <div class="form-group">
            <%= text_field_tag :email, nil, id: "contact-email", class: "form-control", placeholder: "Your Email" %>
          </div>
          <%= submit_tag("Download", class: "btn btn-lg btn-primary", id: "notify-me") %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<%= render "micro/footer" %>

<% if Rails.env.staging? or Rails.env.production? %>
<script type="text/javascript">
  $("#email-submit-form").submit( function( event ) ) {
    analytics.identify($("#contact-email").val(), {
      email: $("#contact-email").val(),
      first_name: $("#contact-name").val(),
      created_at: $.now(),
      plan_name: 'elder law',
      page_location: 'form-submitted'
    });
    return;
  }
</script>
<% end %>

<% end %>
